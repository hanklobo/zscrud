<section id="cta" class="cta">
    <div class="container text-center">
        <h2 class="mb-4">{{__($title)}}</h2>
        <a href="{{$link}}" class="btn btn-light btn-lg">{{__($cta)}}</a>
    </div>
</section>
