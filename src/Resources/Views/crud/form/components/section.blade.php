@if(isset($title))
    <div class="pt-4 col-span-full">
        <h2 class="text-xl font-semibold text-gray-900 dark:text-gray-100 underline underline-offset-1">
            {{ __($title) }}
        </h2>
    </div>
@endif
